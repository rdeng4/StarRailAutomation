# StarRailAutomation
Star Rail copilot

## Development

Use [uv](https://docs.astral.sh/uv/) to manage this project's local `.venv`:

```sh
uv sync --group dev
uv run star-rail-automation
uv run pytest
```
